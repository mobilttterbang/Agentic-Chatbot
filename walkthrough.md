1. Create a virtual environment
2. Install the required packages
3. Add .gitignore and REAMDE.md files
3.1 Add venv/ to .gitignore

4. Connect to the Github Account
4.1 git init
4.2 git add .
4.3 git commit -m "Initial commit"
4.4 git branch -M main
4.5 git remote add origin https://github.com/mobilttterbang/Agentic-Chatbot.git
4.6 git push -u origin main (Update the Remote Repository)

